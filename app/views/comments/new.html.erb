<h1> Add New Comment </h1>
<%= form_with model: comment, url: '/users/:id/posts/:id/', method: :post, local: true, scope: :comment do |f| %>
  <div>
    <%= f.label :text %>
    <%= f.text_field :text %>
  </div>
  <%= f.submit 'Add Comment' %>
<% end %>   